# Course Learning Planner Agent — Dependencies
# MVP: minimal external dependencies for easy setup

# Data validation
pydantic>=2.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# CLI (optional — stdlib argparse is sufficient for MVP)
# rich>=13.0.0

# LLM integration (DeepSeek via OpenAI-compatible API)
openai>=1.0.0
